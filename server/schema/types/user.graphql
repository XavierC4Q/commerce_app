type User {
    username: String!
    password: String!
    email: String!
}

type Query {
    allUsers: [User]
    getUser(username: String!): User
}

type Mutation {
    addUser(username: String!, password: String!, email: String!): User
}