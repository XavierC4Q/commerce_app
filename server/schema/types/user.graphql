type User {
    name: String!
    password: String!
    city: String!
}

type Query {
    allUsers: [User!]
    getUser(name: String!): User
}

type Mutation {
    addUser(name: String!, password: String!, city: String!): User!
}